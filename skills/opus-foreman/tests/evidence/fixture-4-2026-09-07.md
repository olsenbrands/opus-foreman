# Fixture 4 — observed run, 2026-09-07

**OBSERVED, not scripted.** These are transcript lines from a real, billable run on a test machine, not the output of a model or a walkthrough. `tests/fixture-4-grok-transition.sh`
reproduces this run when `FOREMAN_LIVE_GROK=1` is set; without that variable it skips and
costs nothing.

- grok CLI: `grok 1.0.13 (5e9a58528b76) [stable]`
- launcher: `skills/fable-foreman/scripts/grok-dispatch.sh`, **unchanged**, at commit `9b0d090`
- model / effort / sandbox: `grok-4.6` / `low` / step 1 `read-only`, step 2 `workspace`
- scratch repo: outside `/tmp` (the `read-only` profile refuses a workdir beneath `/tmp`)
- raw artifacts at the time of the run:
  `<checkout>/scratch-grok-fixture/.foreman/scratch/a1.json`,
  `a2.json.stderr`

## Step 1 — `read-only` reviewer dispatch

Ticket: read `note.txt`, then attempt to create `PROBE-RO.txt`, and report honestly.

- launcher exit: **0**
- `sessionId`: `<session-id>`
- `stopReason`: `end_turn`; `num_turns`: 2; `total_cost_usd`: `0.00604758`
  (the provider's own accounting for this call, not a bill that was seen)
- `PROBE-RO.txt`: **not created**
- reviewer's report, verbatim from `a1.json`:

> `note.txt` exact contents:
> ```
> hello
> ```
> Write of `PROBE-RO.txt` did not succeed. This session only has read-only tools
> (`read_file`, `list_dir`, `grep`, `search_tool`, `use_tool`). There is no write/edit
> tool, so the file was not created.

## Step 2 — resume that same session id under `workspace`

Same session id, sandbox `workspace`. `a2.json` is zero bytes; `a2.json.stderr` holds,
verbatim:

```
error: cannot resume this session under sandbox profile 'workspace' — it was created with 'read-only'. Omit --sandbox to resume with 'read-only', or start a new session to use 'workspace'.
```

Refused **before any spend** — no model call, no file written, no `PROBE-WS.txt`.

## What this settles

A Grok reviewer-to-fixer transition cannot reuse the reviewer's session: the resume slot
does not cross sandbox profiles. The transition is a **fresh `workspace` dispatch**
carrying the finding, the candidate hash and the criteria verbatim — which is what
`references/grok-workers.md:146` now states as settled rather than conditional.

If a future build permits the cross-profile resume, fixture 4 prints
`OBSERVED CHANGE: cross-profile resume now permitted on this build` and **fails**, so the
documentation is revisited rather than silently left stale.

## Reproduction, same day, by the fixture itself

`FOREMAN_LIVE_GROK=1 sh skills/fable-foreman/tests/fixture-4-grok-transition.sh` — 8 passed,
0 failed. A **new** session (`<session-id>`, step 1 exit 0,
`cost_usd=0.00642022`, `PROBE-RO.txt` not created) hit the identical refusal on step 2:
launcher exit 1, duration 0s, and the same stderr string verbatim. Two independent
sessions, same result — the refusal is the CLI's behaviour, not one session's accident.
